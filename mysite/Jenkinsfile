pipeline {
    agent any 
    stages {
        stage('Build') {
            
              def customImage =  docker.build('djawed22/repo:latest')
              customImage.inside {
                sh 'echo "Hello World"'
              }
            }
        }
}
